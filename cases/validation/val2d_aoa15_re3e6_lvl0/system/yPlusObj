/*--------------------------------*- C++ -*----------------------------------*\
|  Wall y+ function object -- Phase-1 clean-section 2D validation case        |
|  Instantiated from cases/validation_2d/template by                          |
|  scripts/build_validation_case.py                                           |
\*---------------------------------------------------------------------------*/

// Writes the y+ FIELD at every write time plus min/max/average per patch in
// the log. Spec section 4: y+ < 1 everywhere is the design target (the mesh
// generator sizes the first cell from the flat-plate correlation chain in
// scripts/first_cell_height.py), and the case FAILS if max y+ > 2 over more
// than 1% of wall faces -- the post-processing gate computes that histogram
// from the field this object writes.

yPlus1
{
    type            yPlus;
    libs            (fieldFunctionObjects);

    // Field at write times is enough: y+ is a verification quantity, not a
    // convergence monitor.
    writeControl    writeTime;
}

// ************************************************************************* //
