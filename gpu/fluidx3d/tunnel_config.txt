# Glasair VG tunnel configuration -- read by FluidX3D at startup (no rebuild
# needed). Edit and relaunch, or use tunnel.bat <aoa> <re> [mode] [stl].
#
# mode:    wing  = whole 1.5 m section with free tips (pretty, ~4 mm cells)
#          slice = narrow span-periodic strip (science, ~1 mm cells; use a
#                  --span-m 0.25 STL from make_vg_wing.py)
# re:      Reynolds number = the airspeed knob at the 0.902 m aileron chord:
#          stall 80 mph -> 2.2e6 | approach 104 mph -> 2.9e6 | cruise 258 mph -> 7.1e6
# csv:     force-log path; empty disables logging. t_end_steps 0 = run forever.

mode        = wing
stl         = L:/Dev/glasair_vg_sim/gpu/fluidx3d/assets/wing_clean_ail_n_s1.5m.stl
aoa_deg     = 14
re          = 2.2e6
vram_mb     = 6000
u           = 0.10
span_m      = 1.5
chord_m     = 0.9022
autorotate  = 1       # 1 = launch with the built-in camera autorotation already on (R key toggles it any time)
t_end_steps = 0
t_end_si    = 0       # seconds of PHYSICAL airflow, then auto-close (0 = run until closed); ~1.2 s is plenty
video_s     = 0       # >0: export a 360-deg orbit as 60fps PNG frames spanning this many video-seconds (needs t_end_si)
frames_dir  = L:/Dev/glasair_vg_sim/gpu/fluidx3d/results/frames/
log_every   = 1000
csv         = L:/Dev/glasair_vg_sim/gpu/fluidx3d/results/forces_live.csv

# Analytic VG row (stamped into the lattice, not voxelized from STL):
vg_enable   = 1
vg_h_mm     = 10
vg_pitch_mm = 50
vg_t_mm     = 6     # visual thickness; physical is 1.5
vg_beta_deg = 15
